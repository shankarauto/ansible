- name: install packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: install package
    ansible.builtin.debug: 
      name: "{{ item }}"
      state: present
    loop:
    - mysql
    - nginx
    - zip